FROM [from]

ONBUILD ARG HUGO_CMD
ONBUILD ARG HUGO_DESTINATION
ONBUILD ARG HUGO_ENV

ONBUILD ENV HUGO_DESTINATION="${HUGO_DESTINATION:-/onbuild}" \
            HUGO_ENV="${HUGO_ENV:-DEV}"

ONBUILD COPY . /src
ONBUILD RUN hugo ${HUGO_CMD}
